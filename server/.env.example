# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Library Configuration
LIBRARY_PATH=/path/to/your/audiobooks

# Database
DATABASE_PATH=./data/database.sqlite

# Optional: OpenLibrary API (for metadata enrichment)
OPENLIBRARY_ENABLED=false

# PDF Follow-Along / Whisper Configuration
# Model: tiny, tiny.en, base, base.en, small, small.en, medium, medium.en, large
WHISPER_MODEL=base.en
# Windows acceleration flavor: cpu (default), blas (OpenBLAS, faster),
# cublas-11.8 or cublas-12.4 (NVIDIA GPU, requires CUDA toolkit)
WHISPER_WIN_FLAVOR=cpu
